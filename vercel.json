{
    "version": 2,
    "builds": [
        {
            "src": "api/server.ts",
            "use": "@vercel/ts-node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/server.ts"
        }
    ]
}
